/**
 ******************************************************************************
 * @file           : main.c
 * @author         : Auto-generated by STM32CubeIDE
 * @brief          : Main program body
 ******************************************************************************
 * @attention
 *
 * Copyright (c) 2022 STMicroelectronics.
 * All rights reserved.
 *
 * This software is licensed under terms that can be found in the LICENSE file
 * in the root directory of this software component.
 * If no LICENSE file comes with this software, it is provided AS-IS.
 *
 ******************************************************************************
 */

#include <stdint.h>

#if !defined(__SOFT_FP__) && defined(__ARM_FP)
  #warning "FPU is not initialized, but the project is compiling for an FPU. Please initialize the FPU before use."
#endif

int main(void)
{
  /* PA1 Configuration as input */  
  uint32_t  volatile *const pGAModeReg           = (uint32_t*) 0x40020000;
  uint32_t const volatile *const pGAInputReg     = (uint32_t*) 0x40020010;

  /* PE1 Configuration as output */
  uint32_t  volatile *const pGEModeReg           = (uint32_t*) 0x40021000;
  uint32_t  volatile *const pGEOutputReg         = (uint32_t*) 0x40021014;

  uint32_t  volatile *const pCtrlClkRCCreg = (uint32_t*) 0x40023830;
  *pCtrlClkRCCreg |= (1 << 0);
  *pCtrlClkRCCreg |= (1 << 4);

  /* PE1 Configuration Mode */
  *pGEModeReg &= (~(3 << 2));
  *pGEModeReg |= (1 << 2);
  
  /* PA1 Configuration Mode */  
  *pGAModeReg &= (~(3 << 2));

  while (1) {
    uint8_t pinStatus = (uint8_t)(*pGAInputReg & 0x2);

    if (pinStatus) {
      *pGEOutputReg |= (1 << 1);
    }
    else {
      *pGEOutputReg &= (~(1 << 1));
    }
  } 

  return 0;
}
